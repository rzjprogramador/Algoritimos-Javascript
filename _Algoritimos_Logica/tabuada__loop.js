// TABUADA COM LOOP FOR

 // numero a ser afeytado por operacao
 valor = 3

 limiteTabuada = 11
 
 for(i=0; i < limiteTabuada;i++) {
   //console.log(valor + "x" + a + " = " + (valor * a))
   console.log(`${valor} x ${i} = ${(valor * i)}`)
 }

 // TABUADA DO 5
 valor = 5

for(i = 0; i < 11; i++) {
  console.log(`${i} x ${valor} = ${valor * i}`)
}